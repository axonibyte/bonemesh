# BoneMesh Protocol Specification

This folder will hold the protocol specification and its supporting artifacts:

- `v2-behavior.md` — the v2 protocol as actually implemented (M2)
- `protocol.md` / `security.md` — the v3 specification (M3)
- `corpus/` — shared test vectors and hostile-input corpora consumed by every
  implementation
- `conformance/` — the language-agnostic conformance runner

Nothing here yet; see [docs/PLAN.md](../docs/PLAN.md) §Phase 2.
