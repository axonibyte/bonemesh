*~
.DS_Store
# reaper collects run results into <tenant-tree>/out/
out/
erl_crash.dump

# bf/ — the brainfuck port's pinned toolchain, cloned by bf/setup.sh rather
# than vendored. Two copies of a crypto routine are two things that can
# disagree about what the routine is.
/bf/toolchain/
